const Button = {
  variants: {
    bare: {
      color: 'blue',
      fontSize: '14px',
      padding: 0,
      height: '14px',
      backgroundColor: 'transparent',
      '&:hover, &:focus': {
        backgroundColor: 'transparent',
      }
    }
  }
}

export default Button;